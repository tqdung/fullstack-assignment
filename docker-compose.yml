version: "3"
services:
  fullstack-assignment:
    image: fullstack-assignment
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: fullstack-assignment
    ports:
      - "3000:3000"
