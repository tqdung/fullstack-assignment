{
  "scripts": {
    "eslint": "eslint src/{components,pages}/**/*.{ts,tsx} --fix",
    "prettier": "prettier src/{components,pages}/**/*.{ts,tsx} --write",
    "postinstall": "husky install",
    "commit": "git add . && git-cz"
  },
  "devDependencies": {
    "git-cz": "^4.9.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.2",
    "prettier": "^2.8.4"
  }
}
